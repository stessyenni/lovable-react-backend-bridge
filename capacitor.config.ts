
import type { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.lovable.hemapp',
  appName: 'Hemapp',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
